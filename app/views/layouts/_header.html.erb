<nav class="navbar navbar-inverse navbar-fixed-top">
 
  <div class="container-fluid">
      
    <div class="navbar-header" >
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#myNavbar" aria-expanded="false" aria-controls="navbar"> 
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "JasBots", root_path, class: "navbar-brand" %>
     </div>
   <div class="collapse navbar-collapse" id="myNavbar">
   
 <!-- </span> -->
    <ul class="nav navbar-nav">
          <% if signed_in? %>
           
       <li>
          <%= link_to 'Hello, '+ current_user.name ,user_path(current_user) %> </li> <%end%>
        
        <li class="active"><%= link_to "Home", root_path %></li>
        <% if signed_in? %>
        <li><%= link_to "My profile",   user_path(current_user) %></li>
        <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#"> Quick Menu <span class="caret"></span></a>
        <ul class="dropdown-menu">
        <li><%= link_to "Create a bot",   start_path %></li> 
        
         <%end%> </ul></li>
    </ul>

    <ul class="nav navbar-nav navbar-right">
        <li> <% if signed_in? %>
       
      <%= link_to 'Sign out', sign_out_path, method: :delete %> </li>
    <% else %> 
    <li class="dropdown">
      <a href="#" class ="dropdown-toggle" data-toggle="dropdown"><b>Login</b><span class="caret"></span></a>
        <ul id='login-dp' class="dropdown-menu">
          <li>
            <div class="row">
              <div class="col-md-12">
                Login via
                  <div class="social-buttons">
                   <a href="/auth/facebook" class="btn btn-fb"><i class="fa fa-facebook"></i>  Facebook</a>
                  </div> 
                   or

                        <%= form_for :session, html: {class: "form", id:"login-nav"}, url: session_path do |form| %>
                        <div class="form-group">
                          <%= form.label :email, class:"sr-only" %>
                          <%= form.text_field :email, type: 'email', class:"form-control", id:"exampleInputEmail2", placeholder:"Email Address" %>
                        </div>

                        <div class="form-group">
                          <%= form.label :password, class:"sr-only" %>
                          <%= form.password_field :password, class:"form-control", id:"exampleInputPassword2", placeholder:"Password" %>
                        </div>

                        <div class="form-group">
                          <%= form.submit 'Login', class:"btn btn-primary btn-block" %>
                        </div>

                        <div class= "help-block text-right">
                          <%= link_to "Forgot the password?", new_password_path %> 
                        </div>
                      <% end %>

                      <div class="bottom text-center">
                          <% if Clearance.configuration.allow_sign_up? %>
                            <%= link_to "New here?", sign_up_path %>
                          <% end %> 
                      </div>
              </div>
          </div>
          <%end%>
          </li>
      </ul>
     </li>
     </ul>
    </div>
  </div>
</nav>